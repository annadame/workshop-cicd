version: '3.3'

services: 
    jenkins:
        image: terryvdgriend/workshop-jenkins
        ports: 
            - "8081:8080"
        volumes: 
            - "/var/run/docker.sock:/var/run/docker.sock"